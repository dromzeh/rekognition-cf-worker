{
    "scripts": {
        "dev": "wrangler dev src/index.ts",
        "deploy": "wrangler deploy --minify src/index.ts",
        "format": "prettier --write ."
    },
    "dependencies": {
        "@aws-sdk/client-rekognition": "^3.533.0",
        "hono": "^4.1.0",
        "prettier": "^3.2.5",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240208.0",
        "wrangler": "^3.32.0"
    }
}
